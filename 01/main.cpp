#include <iostream>
// iostream 헤더 파일 include
// iostream 헤더 파일은 C++ 표준 입출력에 필요한 것들을 포함하고 있음

int main() {
    std::cout << "Hello world!" << std::endl;
    // std: C++ 표준 라이브러리의 모든 함수, 객체 등이 정의된 이름 공간(namespace)
    // namespace: 정의된 객체의 소속을 정해 줌
    return 0;
}